import { notFound } from 'next/navigation'; import { ArticleCard } from '@/components/article-card'; import { getAllPosts, getCategories } from '@/lib/posts'; export function generateStaticParams(){return getCategories().map(category=>({category:category.toLowerCase()}))} export default async function Category({params}:{params:Promise<{category:string}>}){const {category}=await params;const posts=getAllPosts().filter(p=>p.category.toLowerCase()===category);if(!posts.length)notFound();return <section className="container-page py-14"><p className="text-sm font-bold text-brand">CATEGORY</p><h1 className="mt-2 text-5xl font-black">{posts[0].category}</h1><div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{posts.map(p=><ArticleCard key={p.slug} post={p}/>)}</div></section>}
