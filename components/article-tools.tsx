'use client'; import { ArrowUp, Check, Link2, Share2 } from 'lucide-react'; import { useEffect, useState } from 'react';
export function ArticleTools(){const [progress,setProgress]=useState(0),[copied,setCopied]=useState(false);useEffect(()=>{const f=()=>setProgress(Math.min(100,(scrollY/(document.documentElement.scrollHeight-innerHeight))*100));f();addEventListener('scroll',f,{passive:true});return()=>removeEventListener('scroll',f)},[]);async function copy(){await navigator.clipboard.writeText(location.href);setCopied(true);setTimeout(()=>setCopied(false),1500)} return <><div className="fixed left-0 top-16 z-30 h-1 bg-brand" style={{width:`${progress}%`}}/><div className="fixed bottom-5 right-5 z-30 flex flex-col gap-2"><button onClick={copy} className="glass rounded-full p-3" aria-label="Copy article link">{copied?<Check size={18}/>:<Link2 size={18}/>}</button><button onClick={()=>navigator.share?.({title:document.title,url:location.href})} className="glass rounded-full p-3" aria-label="Share article"><Share2 size={18}/></button><button onClick={()=>scrollTo({top:0,behavior:'smooth'})} className="glass rounded-full p-3" aria-label="Back to top"><ArrowUp size={18}/></button></div></>}
